# Feliz.CompilerPlugin.Tests

This is a pure .NET testing project which MUST not be transpiled by Fable. The Feliz.CompilerPlugin runs during Fable transpilation on .NET runtime, so we need to test it in that same environment.
